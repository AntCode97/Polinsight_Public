spring :
  profiles :
    group :
      dev :  common, devdb, dev
      prod : common, proddb, prod
---
server :
  port : 8080
  servlet :
    context-path :             /
    encoding :
      charset : UTF-8
    application-display-name : dnslab_polinsight
    session :
      cookie :
        http-only : true
  error :
    path : /templates/error

spring :
  servlet :
    multipart :
      max-file-size :    102400KB
      max-request-size : 102400KB

  security :
    oauth2 :
      client :
        provider :
          #          google :
          naver :
            authorization-uri :   https://nid.naver.com/oauth2.0/authorize
            token_uri :           https://nid.naver.com/oauth2.0/token
            user-info-uri :       https://openapi.naver.com/v1/nid/me
            user-name-attribute : response # 네이버에서 반환하는 데이터 구조를 보면 알 수 있음
          kakao :
            authorization-uri :   https://kauth.kakao.com/oauth/authorize
            token-uri :           https://kauth.kakao.com/oauth/token
            user-info-uri :       https://kapi.kakao.com/v2/user/me
            user-name-attribute : id
        registration :
          google :
            clientId :            dda
            clientSecret :        dd
            redirectUriTemplate : "{baseUrl}/oauth2/callback/{registrationId}"
            scope :
              - email
              - profile
          naver :
            client-name :              Naver
            client-id :                dd
            client-secret :            dd
            authorization-grant-type : authorization_code
            redirect-uri :             "{baseUrl}/login/oauth2/code/{registrationId}"
            scope :
              - name
              - email
              - profile_image
          kakao :
            client-name :                  Kakao
            client-id :                    dd
            client-secret :                dd
            authorization-grant-type :     authorization_code
            redirect-uri :                 "{baseUrl}/login/oauth2/code/{registrationId}"
            client-authentication-method : POST
            scope :
              - profile
              - account_email

  mvc :
    static-path-pattern : /static/**
  web :
    resources :
      static-locations : classpath:/static
      add-mappings :     true


  cache :
    type : redis

  mail :
    host :     smtp.gmail.com # smtp address
    port :     587 # smtp port
    username : jnudnslab@gmail.com # smtp 등록 계정
    password : dnslab@3438 # smtp 등록 계정의 패스워드
    properties :
      mail :
        smtp :
          auth : true
          starttls :
            enable : true
  config :
    activate :
      on-profile : common

  output :
    ansi :
      enabled : always
  thymeleaf :
    servlet :
      content-type : "*/*"




file :
  upload :
    baseLocation : "./upload-dir/"

custom :
  permission :
    resources :
      - /css/**
      - /js/**
      - /img/**
      - /favicon.ico

    admin :
      - /admin/**
      - /api/admin/**

    template :
      - /
      - /api/**
      - /signup
      - /join
      - /index
      - /find**
      - /error/**
      - /loginSuccess
      - /login
      - /user/**
      - /findpwd
      - /terms
      - /wholesign
      - /posts**
      - /posts/**
      - /panel
      - /point**
      - /denied
      - /moreinfo
      - /dologin
      - /actuator**
      - /actuator/**
      - /surveys/**
      - /chpwd/**
      - /changepwd**
      - /survey/**
      - /success_basic
      - /basictopanel
      - /panelagreement
      - /success_panel
      - /success_basic
      - /mypage
      - /pointhistories**
      - /point/**
      - /posts
      - /events
      - /qna
      - /faq
      - /api/callback**
  api :
    url :         "https://api.surveymonkey.net/v3/" # 서베이 몽키 API 주소
    accessToken : "uX2r-rEuS8sFuxabPuSDfDkD2KWu459vOQg588cu6qatlX1QS5uURCVTvip1rKzk7ApktG.RWz.vRu69oVmx9WNd.hq0RZ2knTfXf-Fopw7K8f.-EGOgAD-IOXOnFQwH"
  callback :
    base : "http://localhost:8080"
  hash :
    passwordsalt : polinsightPasswordSalt
    pointsalt :    polinsightPointSalt


management :
  endpoints :
    enabled-by-default : true

    web :
      exposure :
        include : "*"
    jmx :
      exposure :
        include : health, info
  endpoint :
    info :
      enabled : true
    health :
      show-components : always
      show-details :    always

---
spring :
  datasource :
    username :          root
    password :          root
    url :               jdbc:mariadb://localhost:3306/polinsight
    driver-class-name : org.mariadb.jdbc.Driver
    hikari :
      pool-name :         Test DBCP Pool
      maximum-pool-size : 5

  redis :
    host : localhost
    port : 6379

  config :
    activate :
      on-profile : devdb
---
spring :
  config :
    activate :
      on-profile : dev
  jpa :
    properties :
      hibernate :
        ddl-auto :   validate
        format_sql : true
    show-sql :     true
    generate-ddl : true

  banner :
    location : banner-dev.txt

  session :
    store-type : none

  devtools :
    restart :
      poll-interval : 1s

  thymeleaf :
    cache : false

server :
  port : 8080
  servlet :
    session :
      timeout : 5m


custom :
  callback :
    base : "http://localhost:8080"
---
spring :
  datasource :
    username :          polinsight
    password :          polinsight
    url :               jdbc:mariadb://pol-mariadb:3306/polinsight?useSSL=false
    driver-class-name : org.mariadb.jdbc.Driver
    hikari :
      maximum-pool-size : 300
      pool-name :         Pollinsight DBCP Pool

  redis :
    host : pol-redis
    port : 6379

  config :
    activate :
      on-profile : proddb

---
spring :
  config :
    activate :
      on-profile : prod
  jpa :
    hibernate :
      ddl-auto : validate
    generate-ddl : false

  banner :
    location : banner-prod.txt
  session :
    store-type : redis
server :
  port : 8080
  servlet :
    session :
      timeout : 15m

logging :
  file :
    name : ./polinsight_logs.log

custom :
  callback :
    base : "http://dnslab.iptime.org"